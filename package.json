{
	"name": "browser-extension",
	"version": "1.0.0",
	"description": "Browser extension template built with Manifest Version 3.",
	"scripts": {
		"test": "TMPDIR=~/tmp-dir/ web-ext run --config=config.js --verbose",
		"test-no-tmp": "web-ext run",
		"build": "web-ext build",
		"lint": "web-ext lint"
	},
	"author": "my-name",
	"license": "MIT",
	"dependencies": {
		"web-ext": "^7.6.2"
	}
}
